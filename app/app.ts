var app = angular.module('app', ['ngRoute']);

// using both routers together works with # routing or with push state routing.
// app.config(($locationProvider) => {
//   $locationProvider.html5Mode(true)
// });
